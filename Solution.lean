import NavierFormal

/-!
# Solutions to the advertised statements (skeleton)

Comparator checks that every declaration named in `comparator.json` has the
same type here as in `Challenge.lean`, and that its proof here uses only the
permitted axioms.
-/

namespace NavierFormal

theorem skeleton_placeholder : (2 : ℕ) + 2 = 4 := by
  norm_num

end NavierFormal
